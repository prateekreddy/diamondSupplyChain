'use strict';

const Boom = require('boom');

/**
 * Operations on /controller/create
 */
module.exports = {
    /**
     * summary: Add a new operator in the diamond supplychain
     * description: 
     * parameters: entity
     * produces: 
     * responses: 405
     */
    get: function (request, h) {
        return Boom.notImplemented();
    }
};
